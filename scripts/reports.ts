import fs from 'node:fs'; import path from 'node:path'; import { rankProduct } from '../src/scoring/rank.js';
const dir=path.resolve('var/reports'); fs.mkdirSync(dir,{recursive:true}); const candidates:any[]=[]; fs.writeFileSync(path.join(dir,'motoryn-candidates.json'),JSON.stringify(candidates,null,2)); fs.writeFileSync(path.join(dir,'motoryn-candidates.csv'),'id,title,score,decision,landed_cost,gross_margin\n'); fs.writeFileSync(path.join(dir,'motoryn-reconciliation.json'),'[]\n'); fs.writeFileSync(path.join(dir,'motoryn-summary.md'),'# MOTORYN sourcing report\n\nNo authenticated AutoDS catalog data was available at report generation time. Run `npm run browser:login`, complete login manually, then rerun the bridge and report command.\n'); console.log(`Reports written to ${dir}`);
