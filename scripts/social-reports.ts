import fs from 'node:fs'; import path from 'node:path'; import { listSocialAccounts, listSocialBrands } from '../src/social/registry.js'; import { officialApiStatus } from '../src/social/adapters.js';
const d=path.resolve('var/reports'); fs.mkdirSync(d,{recursive:true}); fs.writeFileSync(path.join(d,'social-connections.json'),JSON.stringify({brands:listSocialBrands(),accounts:listSocialAccounts(),status:officialApiStatus},null,2)); fs.writeFileSync(path.join(d,'social-capabilities.json'),JSON.stringify({reviewMode:true,autopilot:false,publishingEnabled:false,platforms:officialApiStatus},null,2)); fs.writeFileSync(path.join(d,'social-test-results.json'),JSON.stringify({status:'NOT_CONNECTED',note:'No OAuth credentials or platform API reads configured; no platform is claimed connected.'},null,2)); console.log('Social reports written.');
