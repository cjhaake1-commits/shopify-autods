export type Provenance = 'autods' | 'shopify' | 'derived' | 'unknown';
export interface Sourced<T> { value: T | null; provenance: Provenance; }
export interface AutoDSProduct { id: string; title: string; supplier: Sourced<string>; sourceUrl: Sourced<string>; sourceCost: Sourced<number>; shippingCost: Sourced<number>; currency: string; estimatedDelivery: Sourced<string>; stock: Sourced<number>; variants: unknown[]; images: string[]; videos: string[]; ratings: Sourced<number>; reviewCount: Sourced<number>; orderIndicators: Sourced<number>; automation: { priceMonitoring: boolean|null; stockMonitoring: boolean|null; automaticOrdering: boolean|null; fulfillment: boolean|null; tracking: boolean|null }; raw?: unknown; }
export interface ShopifyProduct { id: string; title: string; handle: string|null; variants: unknown[]; retailPrice: Sourced<number>; compareAtPrice: Sourced<number>; inventory: Sourced<number>; images: string[]; seo: Record<string,string>; status: string|null; vendor: string|null; tags: string[]; collections: string[]; }
export interface Candidate extends AutoDSProduct { category: string; proposedRetail: number|null; landedCost: number|null; grossProfit: number|null; grossMargin: number|null; score: number; decision: 'LAUNCH_CANDIDATE'|'FLAG_REVIEW'; reasons: string[]; }
