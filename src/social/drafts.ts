import type { SocialDraft, SocialPlatform } from './models.js'; export function createDraft(input:{brand:string;platforms:SocialPlatform[];productIds?:string[];campaign?:string;objective?:string;tone?:string}) : SocialDraft { return {id:`draft-${Date.now()}`,brand:input.brand,platforms:input.platforms,productIds:input.productIds??[],campaign:input.campaign??null,objective:input.objective??'engagement',status:'DRAFT',content:{tone:input.tone??'clear and useful',reviewRequired:true},provenance:['draft generated from supplied inputs; no platform API call']}; }
