import type { AutoDSProduct, Candidate } from '../models/types.js'; import { landedCost, grossProfit, grossMargin, suggestedRetail } from './margin.js';
const terms=['vacuum','inflator','carplay','android auto','jump starter','obd','dash cam','tpms','hud','gps','automotive','car'];
export function rankProduct(p:AutoDSProduct, target=.6):Candidate { const cat=terms.find(t=>p.title.toLowerCase().includes(t)) ?? 'automotive technology'; const lc=landedCost(p.sourceCost.value,p.shippingCost.value); const retail=suggestedRetail(lc,target); const gp=grossProfit(retail,lc); const gm=grossMargin(retail,lc); let score=0; const reasons:string[]=[]; if(lc!=null){score+=20;reasons.push('cost evidence')} if(gm!=null&&gm>=target){score+=25;reasons.push('target margin')} if(p.stock.value==null||p.stock.value>0){score+=15;reasons.push('stock available/unknown')} if(p.images.length>=3){score+=15;reasons.push('useful media')} if(p.videos.length){score+=10;reasons.push('video media')} if(p.ratings.value!=null){score+=5;reasons.push('rating evidence')} if(p.reviewCount.value!=null){score+=5;reasons.push('review evidence')} if(terms.some(t=>p.title.toLowerCase().includes(t))){score+=5;reasons.push('automotive relevance')} return {...p,category:cat,proposedRetail:retail,landedCost:lc,grossProfit:gp,grossMargin:gm,score,decision: score>=70?'LAUNCH_CANDIDATE':'FLAG_REVIEW',reasons}; }
