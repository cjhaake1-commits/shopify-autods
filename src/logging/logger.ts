export const logger={ info:(event:string,data:Record<string,unknown>={})=>console.log(JSON.stringify({level:'info',event,...data})), warn:(event:string,data:Record<string,unknown>={})=>console.warn(JSON.stringify({level:'warn',event,...data})), error:(event:string,data:Record<string,unknown>={})=>console.error(JSON.stringify({level:'error',event,...data})) };
