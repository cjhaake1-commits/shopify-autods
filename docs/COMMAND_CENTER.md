# Command center

```powershell
npm.cmd run command-center
npm.cmd run command-center:status
npm.cmd run command-center:stop
```

The command center keeps CDP loopback-only and starts the read-only MCP service. It does not start social publishing, schedulers, tunnels, or write tools.
