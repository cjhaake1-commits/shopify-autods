# AutoDS setup

Use the dedicated normal Edge profile with `npm.cmd run edge:cdp`. Authenticate manually. The bridge targets explicit verified IDs: MOTORYN `5669701`, OMNIFINDA `5669573`. Product reports are cache fallbacks and are labeled with provenance; live structured responses take precedence when an authenticated page is available.
